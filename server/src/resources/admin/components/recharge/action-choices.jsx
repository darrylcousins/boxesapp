/**
 * Figure out the modal options for moving a product
 *
 * @module app/recharge/select-choices
 */

import { createElement, Fragment } from "@b9g/crank";

export const getActionModalOptions = ({}) => {
    let [
      modalNote,
    ] = Array(1).fill(null);
    return {
      modalNote,
    };
  };
